[pytest]
testpaths = tests
# The suite runs with /app (= backend/) as the working directory, so `app` is
# the package under test.
pythonpath = .
addopts = -q --strict-markers
markers =
    slow: builds a full mesh or spawns processes — seconds, not milliseconds
