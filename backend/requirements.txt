fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.34.0
python-multipart==0.0.32

# M2: DEM fetch/decode + mesh generation + export
numpy==2.2.1
pillow==12.3.0
requests==2.33.0
trimesh==4.5.3
scipy==1.15.0
networkx==3.4.2
lxml==6.1.0

# M6: PLATEAU LOD2 building polygon triangulation
mapbox-earcut==1.0.3

# Printability massing: the 2D outline algebra the road grooves and the
# nameplate still work in (core/roads.py, core/nameplate.py)
shapely==2.0.6

# Printability massing in 3D (core/voxel.py): marching cubes over the signed
# distance of the occupancy lattice. scipy supplies the distance transforms.
scikit-image==0.25.0

# 銘板 (nameplate): uploaded SVG -> path outlines (core/nameplate.py)
svgelements==1.9.6

# 裏面の出典刻印 (core/stamp.py): font glyph outlines for the debossed credit
freetype-py==2.5.1
